<% provide( :page_header, @item.name ) %>

<div class="form-group col-xs-12">
  <label class="col-xs-2 control-label">Base Fee</label>
  <div class="form-control-information col-xs-10"><%= number_to_currency @item.item_type_base_fee %></div>
</div>

<div class="form-group col-xs-12">
  <label class="col-xs-2 control-label">Fee Per Day</label>
  <div class="form-control-information col-xs-10"><%= number_to_currency @item.item_type_fee_per_day %></div>
</div>

<div class="form-group col-xs-12">
  <div class="col-xs-offset-2 col-xs-10">
    <%= link_to 'Edit Item', edit_item_path(@item), class: "btn btn-primary" %>
    <%= link_to 'Delete Item', { :controller => "items", :action => "destroy" }, method: :delete, data: { :confirm => 'Are you sure?' }, class: 'btn btn-danger' %>
    <%= link_to 'Back', session[:return_to] || items_path, class: "btn btn-default" %>
  </div>
</div>
