<% provide(:page_header, 'Holds') %>

<%= link_to 'New Hold', new_hold_path, class: "btn btn-primary" %>
<br><br>

<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr>
      <th>Reason for Hold</th>
      <th>Item Type</th>
      <th>Item</th>
      <th>Start Time</th>
      <th>End Time</th>
      <th></th>
    </tr>
  </thead>
    <% @holds.each do |hold| %>
      <tr>
        <td><%= hold.hold_reason %></td>
        <td><%= hold.item_type.name %></td>
        <td><%= hold.item.name %></td>
        <td><%= hold.start_time.to_date %></td>
        <td><%= hold.end_time.to_date %></td>
        <td>
            <%= link_to (content_tag(:span, nil, class: "glyphicon glyphicon-eye-open")+" View"), hold, class: "btn btn-xs btn-success" %>
            <%= link_to (content_tag(:span, nil, class: "glyphicon glyphicon-edit")+" Edit"), edit_hold_path(hold), class: "btn btn-xs btn-primary" %>
            <%= link_to (content_tag(:span, nil, class: "glyphicon glyphicon-ban-circle")+"Delete"), hold, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-xs btn-danger" %>
        </td>
      </tr>
    <% end %>
  <tbody>
  </tbody>
</table>
